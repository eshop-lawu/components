<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                                http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">


       <aop:aspectj-autoproxy proxy-target-class="true" />

       <!-- 配置切面的Bean -->
       <bean id="businessDecisionAspect" class="com.lawu.concurrentqueue.bizctrl.BusinessDecisionAspect"></bean>

       <aop:config>
              <aop:pointcut id="pointcut" expression="@annotation(com.lawu.concurrentqueue.bizctrl.annotation.BusinessInventoryCtrl)" />
              <aop:aspect id="concurrentQueue" ref="businessDecisionAspect">
                     <aop:around method="aroundMethod" pointcut-ref="pointcut"/>
              </aop:aspect>
       </aop:config>

       <bean id="concurrentTaskExecutor" class="com.lawu.concurrentqueue.requestctrl.ConcurrentTaskExecutor" >
              <property name="corePoolSize" value="${lawu.concurrent.queue.corePoolSize}" />
              <property name="maximumPoolSize" value="${lawu.concurrent.queue.maximumPoolSize}" />
              <property name="keepAliveTime" value="${lawu.concurrent.queue.keepAliveTime}" />
              <property name="queueCount" value="${lawu.concurrent.queue.queueCount}" />
       </bean>
</beans>